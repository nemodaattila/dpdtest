<?php

/**
 * ROOT directory of the project
 */
DEFINE("ROOT", $_SERVER['DOCUMENT_ROOT']);

/**
 * the width of a corner item in meters (0,5 + 0,5)
 */
const CORNER_WIDTH_IN_METER = 1;

/**
 * the width of column in meters
 */
const COLUMN_WIDTH_IN_METER = 0.2;

/**
 * the width of a wire-fence in meters
 */
const WIRE_WIDTH_IN_METER = 2;

/**
 * the width of a gate in meters
 */
const GATE_WIDTH_IN_METER = 5;

/**
 * the length of a geographic decimal degree AT the equator
 */
const ONE_DECIMAL_DEGREE_IN_METER = 111320;

/**
 * count of gates in the whole fence
 */
const GATE_COUNT = 4;

/**
 * count of corners in the whole fence
 */
const CORNER_COUNT = 4;

/**
 * the price of one corner item in euro
 */
const CORNER_PRICE = 400;

/**
 * the price of one column item in euro
 */
const COLUMN_PRICE = 100;

/**
 * the price of one wire-fence item in euro
 */
const WIRE_PRICE = 50;

/**
 * the price of one gate item in euro
 */
const GATE_PRICE = 1000;

